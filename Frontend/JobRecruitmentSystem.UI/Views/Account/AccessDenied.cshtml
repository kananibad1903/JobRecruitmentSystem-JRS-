@{
    ViewData["Title"] = "Access Denied";
}

<div class="text-center py-5">
    <h1 class="display-6">🚫 @Loc.T("errors.accessDenied")</h1>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">@Loc.T("nav.home")</a>
</div>
