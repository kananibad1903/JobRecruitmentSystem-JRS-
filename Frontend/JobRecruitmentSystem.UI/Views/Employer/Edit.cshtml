@model JobPostFormViewModel
@{
    ViewData["Title"] = Loc.T("employer.editTitle");
}

<h1 class="h3 mb-4"><i class="bi bi-pencil-square text-primary me-2"></i>@Loc.T("employer.editTitle")</h1>

<form asp-controller="Employer" asp-action="Edit" asp-route-id="@Model.Id" method="post" class="js-loading-form">
    <partial name="_JobPostForm" />
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
